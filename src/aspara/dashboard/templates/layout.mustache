<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{page_title}} - Aspara</title>
  <link rel="icon" href="/static/favicon.ico" type="image/x-icon">
  <link rel="shortcut icon" href="/static/favicon.ico" type="image/x-icon">
  <link rel="stylesheet" href="/static/dist/css/styles.css">
  <link rel="stylesheet" href="/static/css/tagger.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background-color: #F5F3F0;
      color: #2C2520;
      line-height: 1.6;
      letter-spacing: -0.01em;
    }
  </style>
</head>
<body class="text-text-primary" {{#read_only}}data-read-only="true"{{/read_only}}>
  {{> _icons}}

  <div class="flex flex-col min-h-screen">
    <!-- Navigation Bar -->
    <nav class="bg-base-surface border-b border-base-border">
      <div id="nav-container" class="max-w-7xl mx-auto px-6 py-5">
        <div class="flex items-center justify-between">
          <a href="/" class="flex items-center font-semibold text-xl text-text-primary tracking-tight hover:text-accent transition-colors">
            <img src="/static/images/aspara-icon.png" alt="Aspara Logo" class="w-8 h-8 mr-3">
            Aspara
          </a>

          <!-- Settings Menu -->
          <div id="settings-menu" class="relative">
            <button id="settings-menu-button"
                    class="w-8 h-8 flex items-center justify-center border border-base-border rounded-md bg-base-surface text-text-secondary hover:bg-base-bg hover:text-text-primary transition-colors"
                    title="Settings">
              <svg class="w-5 h-5"><use href="#icon-menu"></use></svg>
            </button>

            <!-- Dropdown Menu -->
            <div id="settings-menu-dropdown" class="absolute right-0 top-full mt-2 w-56 bg-base-surface border border-base-border rounded-md shadow-lg z-50 transition-all duration-150 ease-out origin-top-right opacity-0 scale-95 pointer-events-none">
              <div class="p-2">
                <label class="flex items-center gap-3 px-3 py-2 cursor-pointer hover:bg-base-bg rounded transition-colors">
                  <input type="checkbox" id="fullWidthToggle" class="w-4 h-4 accent-accent">
                  <span class="text-sm text-text-primary">Full Width Mode</span>
                </label>
              </div>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <!-- Notification Container -->
    <div id="notification-container" class="fixed top-20 left-1/2 transform -translate-x-1/2 z-50 space-y-2"></div>

    <!-- Main Content -->
    <div class="flex-grow w-full">
      <div id="main-content-container" class="max-w-7xl mx-auto py-8 px-6">
        {{{content}}}
      </div>
    </div>

    <!-- Footer -->
    <footer class="border-t border-base-border mt-16 bg-base-surface">
      <div class="max-w-7xl mx-auto py-6 px-6 text-center text-xs text-text-muted">
        Aspara Metrics Tracker
      </div>
    </footer>
  </div>
  {{> _delete_dialog}}
  {{> _read_only_dialog}}
  <script type="module" src="/static/dist/settings-menu.js"></script>
  <script type="module" src="/static/dist/note-editor.js"></script>
  <script type="module" src="/static/dist/tag-editor.js"></script>
  <script type="module" src="/static/dist/components/delete-dialog.js"></script>
  <script type="module" src="/static/js/notifications.js"></script>
</body>
</html>
